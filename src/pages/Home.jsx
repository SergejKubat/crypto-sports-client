import React from "react";

import Search from "../components/Search/Search";

const HomePage = () => {
    return (
        <section>
            <Search />
        </section>
    );
};

export default HomePage;
